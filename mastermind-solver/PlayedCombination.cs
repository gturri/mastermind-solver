namespace mastermind_solver;

public class PlayedCombination(Combination combination, CombinationResult result)
{
    public readonly Combination Combination = combination;
    public readonly CombinationResult Result = result;
}